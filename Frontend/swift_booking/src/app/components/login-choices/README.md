# Login Choice Components

The Login Choice Components are normally accessed from the AuthGuard when a user attempts to access a protected route without being authenticated.

The Login Choice Components allow users to select their preferred login method. They provide options for logging in as a regular user or as a member, and handle the redirection to the appropriate authentication flow based on the user's choice.